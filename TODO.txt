## Harshil and Var

Add an explanation (where) about how sometimes we make a `.gitignore` by hand while, other times, we let some other program either create the file from scratch or modify a `.gitignore` we have already created. In a different knowledge drop, explain --- in one sentence --- what the jngb (whatever) file extension refers.

Do 09-infrastructure.


## Preceptor

Need to ensure that, by the time we finish the core VS Code tutorials that students are very comfortable with repo, .gitginore, QMD and so on. That is, we want to guarantee that all later tutorials can start with one question that starts (and states) from the assumption that you are in a repo named `x` and which tells you to set up a QMD, .gitigore with *_files, commit/push. And then the rest of the tutorial just has the student telling AI what to do with the QMD.


Need to end infrastructure tutorial (or add another tutorial) with a topic for creating your own `codespace-starter`, maybe called `starter`. Key is to change the settings so that this compiles quickly, and then students can launch it fast and then (how) turn it into a new repo. Must be possible for this process to be fast.

## Priorities

Maybe a different tutorial which walked through a bunch of different Quarto project types, not because we are especially interested in Dashboards, but because we want to give students more practice dealing with projects with lots of different files doing different things.

### Other Stuff

Tutorial times from 2025

# A tibble: 7 × 3
  source                minutes     N
  <fct>                   <dbl> <int>
1 01-code_answers          119     68
2 02-quarto_answers         90     50
3 03-terminal_answers      112.    46
4 04-github-1_answers      120     53
5 05-github-2_answers       60     44
6 06-websites-1_answers     60     39
7 07-websites-2_answers    120     33

Update images in 01-code.

Should longer tutorials like 1, 4 and 7 have one of their exercises removed?

* Finish skeleton in tutorial.helpers

* Finish "Tutorials in the Age of AI" in tutorial.helpers.

* Rewrite "Instructions"

* Do we need an AI Getting Started tutorial which would highlight things like don't use install.packages() in your scripts and other generic lessons for working with AI? Maybe this is an "Introduction to AI" tutorial in tutorial.helpers?


* Keep package help page and README page in sync better.

* What do we do with skeleton.Rmd in tutorial.helpers package?


* Add how to delete a repo, and then delete one of the ones you made.

For when we revisit the details of the terminal: https://code.visualstudio.com/docs/terminal/basics

## Things to do earlier

In total, we make 5 websites. We need to make their construction more consistent, making things simpler in the initial ones and more complex, with more skipped steps, in later ones. Students will probably be doing all these within a day or two.

## Other items

To shrink the size of all the PNG files, do:

find . -type f -name "*.png" -exec pngquant "{}" -o "{}" --force \;

We could do all the example repos in PPBDS and then leave them up forever, so they would be available to students doing the work. Or for a ppbds-student account.
